/**
 * @file events.ts
 * @description Utilidades para crear, firmar y parsear eventos Nostr
 *
 * TODO: Implementar funciones:
 * - createEvent(kind, content, tags): Crear evento base
 * - signEvent(event, privateKey): Firmar evento
 * - verifyEvent(event): Verificar firma de evento
 * - encryptDM(content, recipientPubkey): Encriptar mensaje directo (NIP-04)
 * - decryptDM(event, privateKey): Desencriptar mensaje directo
 * - parseEventContent<T>(event): Parsear contenido JSON de evento
 */

export {};
