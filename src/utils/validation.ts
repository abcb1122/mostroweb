/**
 * @file validation.ts
 * @description Utilidades para validación de datos
 *
 * TODO: Implementar validadores:
 * - validateOrderAmount(amount, min, max): Validar monto de orden
 * - validatePremium(premium): Validar prima (porcentaje)
 * - validatePaymentMethod(method): Validar método de pago
 * - validateCurrency(currency): Validar código de moneda
 * - validateInvoice(invoice): Validar Lightning invoice
 * - validateNostrEvent(event): Validar estructura de evento
 *
 * - Form validators:
 *   - isValidAmount(value): ¿Monto válido?
 *   - isValidRange(min, max): ¿Rango válido?
 *   - isRequiredField(value): Campo requerido
 */

export {};
